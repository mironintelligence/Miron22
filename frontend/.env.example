# Copy to .env.local for local dev
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_URL=http://127.0.0.1:8000
# Production should normally leave VITE_API_BASE_URL empty and use vercel.json
# rewrites to the Cloudflare gateway at https://api.mironintelligence.com.
# Dokümantasyon: backend ADMIN_PANEL_PASSWORD ile aynı olmalı (doğrulama sunucuda).
VITE_ADMIN_PASSWORD=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
